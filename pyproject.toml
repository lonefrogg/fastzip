[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "fastzip"
version = "1.0.0"
description = "Fast zipping tool"
authors = [
    {name = "Lonefrogg"}
]

[project.scripts]
fastzip = "fastzip.cli:main"